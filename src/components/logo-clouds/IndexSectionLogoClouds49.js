import React from 'react';

export default function IndexSectionLogoClouds49() {
    return (
        <React.Fragment>
            <>
                <style dangerouslySetInnerHTML={{__html: "\n        @keyframes scroll {\n          0% { transform: translateX(0%); }\n          100% { transform: translateX(-100%); }\n        }\n      \n        .animate-scroll {\n          animation: scroll 20s linear infinite;\n        }\n      " }} />


            </>
        </React.Fragment>
    );
}

